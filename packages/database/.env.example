# schema.prisma's datasource is MySQL — Hostinger only provisions MySQL, not
# Postgres (see packages/database/prisma/schema.prisma's own note). Point
# this at a local MySQL/MariaDB instance for dev, and at the Hostinger-
# provisioned database in production.
DATABASE_URL="mysql://user:password@localhost:3306/gch_dev"
