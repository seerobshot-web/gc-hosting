# Local dev default — SQLite, so `prisma migrate dev` runs with no external
# database server. Point this at a real Postgres instance in staging/prod
# and switch schema.prisma's datasource provider to "postgresql".
DATABASE_URL="file:./dev.db"
