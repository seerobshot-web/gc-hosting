# WordPress Blueprint

Approved theme boilerplate, plugin lockfile, and security rules for
WordPress deliverables.

## Checklist
- [ ] Use the approved theme boilerplate (add its repo/location here once
      selected — none is locked in yet)
- [ ] Install only plugins present in the plugin lockfile (add lockfile
      path here once one exists)
- [ ] Apply standard security hardening rules (add the rule set here)
- [ ] Provision hosting via the standard ResellPortal order flow
- [ ] Apply brand tokens from `packages/ui` to the theme's customizer

## Open items
This blueprint is a placeholder structure — the actual theme boilerplate,
plugin lockfile, and security rule set referenced above don't exist yet and
need to be authored before this is delivery-ready.
