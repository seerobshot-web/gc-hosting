---
import Layout from "../layouts/Layout.astro";

const baseUrl = import.meta.env.BASE_URL;
const modules = [
  {
    type: "LINK",
    label: "Sunday livestream",
    description: "Feature the weekly stream at the top of the page.",
    status: "Live",
  },
  {
    type: "EVENT",
    label: "Fall revival weekend",
    description: "Promote registration and schedule details.",
    status: "Scheduled",
  },
  {
    type: "GIVING_EXTERNAL",
    label: "Online giving",
    description: "Point visitors to the secure giving platform.",
    status: "Connected",
  },
  {
    type: "ANNOUNCEMENT",
    label: "Volunteer sign-ups",
    description: "Highlight current serving opportunities.",
    status: "Draft",
  },
];
---

<Layout title="Dashboard Preview | Glory Cloud Hosts">
  <main class="min-h-screen bg-cloudlight">
    <section class="border-b border-ash-stone/80 bg-white/70">
      <div class="mx-auto flex max-w-6xl items-center justify-between px-6 py-4">
        <div>
          <p class="text-xs uppercase tracking-[0.25em] text-verdigris-sky">
            GitHub Pages Preview
          </p>
          <h1 class="font-display text-3xl text-hearth-ink">
            GloryLink Dashboard
          </h1>
        </div>
        <a
          href={baseUrl}
          class="rounded-md border border-ash-stone px-4 py-2 text-sm font-medium text-hearth-ink transition hover:border-ember-core hover:text-ember-core"
        >
          Back to site
        </a>
      </div>
    </section>

    <section class="mx-auto grid max-w-6xl gap-6 px-6 py-10 lg:grid-cols-[220px_minmax(0,1fr)]">
      <aside class="rounded-2xl border border-ash-stone bg-white p-5 shadow-sm">
        <p class="text-xs uppercase tracking-[0.25em] text-verdigris-sky">
          Workspace
        </p>
        <h2 class="mt-2 font-display text-2xl text-hearth-ink">
          New Hope Church
        </h2>
        <p class="mt-2 text-sm text-hearth-ink/70">
          Static preview for sharing layout and content hierarchy before wiring
          live auth and API data.
        </p>

        <nav class="mt-6 space-y-2 text-sm">
          <div class="rounded-lg bg-hearth-ink px-3 py-2 font-medium text-cloudlight">
            GloryLink modules
          </div>
          <div class="rounded-lg px-3 py-2 text-hearth-ink/70">
            Branding
          </div>
          <div class="rounded-lg px-3 py-2 text-hearth-ink/70">
            Analytics
          </div>
          <div class="rounded-lg px-3 py-2 text-hearth-ink/70">
            Publishing
          </div>
        </nav>
      </aside>

      <div class="space-y-6">
        <div class="grid gap-4 md:grid-cols-3">
          <article class="rounded-2xl border border-ash-stone bg-white p-5 shadow-sm">
            <p class="text-xs uppercase tracking-[0.25em] text-verdigris-sky">
              Active modules
            </p>
            <p class="mt-3 font-display text-4xl text-hearth-ink">4</p>
          </article>
          <article class="rounded-2xl border border-ash-stone bg-white p-5 shadow-sm">
            <p class="text-xs uppercase tracking-[0.25em] text-verdigris-sky">
              Published
            </p>
            <p class="mt-3 font-display text-4xl text-hearth-ink">3</p>
          </article>
          <article class="rounded-2xl border border-ash-stone bg-white p-5 shadow-sm">
            <p class="text-xs uppercase tracking-[0.25em] text-verdigris-sky">
              Last updated
            </p>
            <p class="mt-3 text-lg font-medium text-hearth-ink">Today</p>
          </article>
        </div>

        <section class="rounded-2xl border border-ash-stone bg-white p-6 shadow-sm">
          <div class="flex flex-col gap-2 border-b border-ash-stone pb-4 md:flex-row md:items-end md:justify-between">
            <div>
              <p class="text-xs uppercase tracking-[0.25em] text-verdigris-sky">
                Preview data
              </p>
              <h2 class="font-display text-2xl text-hearth-ink">
                Module lineup
              </h2>
            </div>
            <p class="text-sm text-hearth-ink/70">
              Mirrors the intended dashboard presentation without requiring a
              live session.
            </p>
          </div>

          <ul class="mt-6 space-y-4">
            {
              modules.map((module) => (
                <li class="rounded-xl border border-ash-stone bg-cloudlight px-4 py-4">
                  <div class="flex flex-col gap-3 md:flex-row md:items-start md:justify-between">
                    <div>
                      <p class="text-xs uppercase tracking-[0.2em] text-verdigris-sky">
                        {module.type.replace("_", " ")}
                      </p>
                      <h3 class="mt-1 text-lg font-medium text-hearth-ink">
                        {module.label}
                      </h3>
                      <p class="mt-1 text-sm text-hearth-ink/70">
                        {module.description}
                      </p>
                    </div>
                    <span class="inline-flex rounded-full bg-ember-core px-3 py-1 text-xs font-semibold uppercase tracking-[0.18em] text-cloudlight">
                      {module.status}
                    </span>
                  </div>
                </li>
              ))
            }
          </ul>
        </section>
      </div>
    </section>
  </main>
</Layout>
